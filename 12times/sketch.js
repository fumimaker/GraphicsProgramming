//var posX;
//var posY;
var pos;

function setup(){
  createCanvas(windowWidth, windowHeight);
  //posX = random(width);
  //posY = random(height);

  pos = createVector(random(width),randon(height));
}

function draw(){

}